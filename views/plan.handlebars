<div class="container">
    {{> button href="/plan/add" btnclass="btn btn-secondary w-100 my-4" cta="Generate new Plan!"}}
</div>
{{#each plans}}
    <div class="plan-container container border-bottom position-relative mb-4" data-id="{{this._id}}">
        {{> plantile plan=this}}
    </div>
{{/each}}

<script src="./js/components/plan.js"></script>

<style>
    .delete-right {
        transform: scaleY(0);
        opacity: 1;
        overflow: hidden;
        transition: all 300ms ease-in-out;
    }

    .js-delete-plan {
        color: #f7996e;
    }
</style>

{{> bottombar}}